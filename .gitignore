node_modules/
dist/
.DS_Store
*.local

# I sorgenti pesanti degli asset stanno tutti **fuori dal repo**, accanto a
# esso: `../scena 3d/` per i PSD e `../asset-lavoro/` per i PNG della prima
# scena. Ciò che serve al sito è rigenerabile da lì con `tools/prep-*.mjs`.
asset-lavoro/
